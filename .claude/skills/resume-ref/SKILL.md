# resume-ref

이력서 문장 패턴 참고 및 자동 생성 스킬

## 사용법

```bash
# Reference 문서 관리
/resume-ref analyze           # Rallit 이력서 분석 → Reference 생성 (수동)

# Reference 문서 보기
/resume-ref patterns          # 문장 패턴 보기
/resume-ref verbs             # 동사 사전 보기
/resume-ref metrics           # 정량적 성과 표현 보기

# 이력서 생성
/resume-ref generate          # 대화형 이력서 생성
/resume-ref generate --from-git   # Git 커밋 기반 생성
/resume-ref generate --from-sessions   # 세션 기반 생성

# 기존 이력서 개선
/resume-ref improve <path>    # 문장 개선 제안
```

## 실행 단계

### 인자 파싱

- `analyze`: Rallit 이력서 병렬 분석 → Reference 생성 (수동)
- `patterns`: sentence-patterns.md 내용 출력
- `verbs`: action-verbs.md 내용 출력
- `metrics`: metrics-examples.md 내용 출력
- `generate`: 대화형 이력서 생성
- `generate --from-git`: Git 기반 생성
- `generate --from-sessions`: 세션 기반 생성
- `improve <path>`: 기존 이력서 개선

---

### Case 0: `analyze` - Reference 문서 생성 (수동)

Rallit 인기 이력서를 분석하여 Reference 문서를 생성합니다.

**주의: 이 작업은 수동으로만 실행합니다.**

**실행 방법**:

1. 사용자가 `/resume-ref analyze` 요청
2. 메인 Claude가 Rallit URL 5개 수집 (인기순 개발자 이력서)
3. 서브에이전트 병렬 호출 없이, 단순화된 분석 진행:
   - URL을 하나씩 읽고 패턴 추출
   - 공통 패턴 정리
4. Reference 문서 4개 생성/갱신:
   - `sentence-patterns.md`
   - `action-verbs.md`
   - `metrics-examples.md`
   - `section-guidelines.md`

**Reference 갱신 안내**:

```markdown
Reference 문서가 오래되었거나 없다면:

1. /resume-ref analyze 실행
2. Rallit 이력서 5개 분석
3. Reference 문서 자동 생성
```

---

### Case 1: `patterns` - 문장 패턴 보기

Reference 폴더의 sentence-patterns.md 파일을 읽어서 출력합니다.

```bash
SKILL_DIR="$(dirname "$0")"
cat "$SKILL_DIR/reference/sentence-patterns.md"
```

---

### Case 2: `verbs` - 동사 사전 보기

```bash
SKILL_DIR="$(dirname "$0")"
cat "$SKILL_DIR/reference/action-verbs.md"
```

---

### Case 3: `metrics` - 정량적 성과 표현 보기

```bash
SKILL_DIR="$(dirname "$0")"
cat "$SKILL_DIR/reference/metrics-examples.md"
```

---

### Case 4: `generate` - 대화형 이력서 생성

**Step 1**: 기본 정보 수집 (AskUserQuestion)

```
Q: 직군이 무엇인가요?
   - 백엔드 개발자
   - 프론트엔드 개발자
   - 풀스택 개발자
   - 데브옵스/인프라

Q: 경력 연차는?
   - 1~3년 (주니어)
   - 4~7년 (미드레벨)
   - 8년 이상 (시니어)

Q: 주력 기술 스택은? (자유 입력)
```

**Step 2**: 경력/프로젝트 정보 수집

```
Q: 현재/이전 회사와 담당 업무를 알려주세요.
   (자유 형식으로 입력, AI가 구조화)

Q: 가장 임팩트 있었던 프로젝트나 성과가 있다면?
```

**Step 3**: Reference 문서 참조하여 문장 생성

- `sentence-patterns.md` 패턴 적용
- `action-verbs.md` 강한 동사 사용
- `metrics-examples.md` 정량화 제안

**Step 4**: 이력서 md 파일 생성

```bash
OUTPUT_FILE="./resume-draft-$(date +%Y-%m-%d).md"
```

---

### Case 5: `generate --from-git` - Git 기반 생성

**Step 1**: Git 히스토리 수집

```bash
# 최근 6개월 커밋
git log --oneline --since="6 months ago" --author="$(git config user.name)"

# 커밋 메시지 분석
# feat: → 새 기능 개발
# fix: → 버그 수정/개선
# refactor: → 코드 품질 개선
# perf: → 성능 최적화
```

**Step 2**: 프로젝트별 그룹핑

- 저장소명으로 프로젝트 분류
- 주요 성과 추출

**Step 3**: Reference 패턴 적용

```
입력: "fix: 정산 쿼리 N+1 문제 해결"
출력: "정산 쿼리의 N+1 문제를 Batch Fetching으로 해결하여 API 응답 시간 60% 단축"
```

**Step 4**: 이력서 md 파일 생성

---

### Case 6: `improve <path>` - 이력서 개선

**Step 1**: 기존 이력서 파싱

```bash
FILE_PATH="$1"
if [[ ! -f "$FILE_PATH" ]]; then
  echo "❌ 파일을 찾을 수 없습니다: $FILE_PATH"
  exit 1
fi
```

**Step 2**: 문장 분석

- 약한 동사 감지 (만들었다, 했다, 고쳤다)
- 정량적 성과 누락 감지
- 패턴 일치 여부 확인

**Step 3**: 개선 제안 출력

```markdown
## 이력서 개선 제안

### 1. 자기소개 섹션

| 현재                         | 개선안                             | 이유                  |
| ---------------------------- | ---------------------------------- | --------------------- |
| "로그인 기능을 개발했습니다" | "JWT 기반 인증 시스템을 설계/구축" | 약한 동사 → 강한 동사 |

### 2. 정량적 성과 추가 권장

- "API 개발" → "일 100만 요청 처리하는 API 개발"
```

---

## Reference 문서 위치

```
.claude/skills/resume-ref/
├── SKILL.md (이 파일)
└── reference/
    ├── sentence-patterns.md    # 문장 패턴 모음
    ├── action-verbs.md         # 동사/표현 사전
    ├── metrics-examples.md     # 정량적 성과 표현
    └── section-guidelines.md   # 섹션별 가이드
```

---

## 서브 에이전트 프롬프트 템플릿

레퍼런스 문서(action-verbs, sentence-patterns, metrics-examples, section-guidelines)를 직접 적용하도록 설계된 서브 에이전트 프롬프트입니다. 메인 Claude가 호출 시 아래 플레이스홀더에 실제 내용을 주입해 사용하세요.

공통 컨텍스트 (플레이스홀더):

```
CONTEXT.role: 목표 직군 (예: 백엔드)
CONTEXT.seniority: 주니어/미드/시니어
CONTEXT.target_role: 지원 포지션 (예: 시니어 백엔드)
CONTEXT.lang: ko|en (기본: ko)
REFERENCE.action_verbs: reference/action-verbs.md 내용
REFERENCE.sentence_patterns: reference/sentence-patterns.md 내용
REFERENCE.metrics: reference/metrics-examples.md 내용
REFERENCE.section_guidelines: reference/section-guidelines.md 내용
```

---

### Prompt A: 세션/텍스트 → 성과 후보 추출

```
## 작업: 이력서 성과 후보 추출 및 점수화

컨텍스트
- Role: {{CONTEXT.role}} / Seniority: {{CONTEXT.seniority}} / Target: {{CONTEXT.target_role}}
- 언어: {{CONTEXT.lang}}

참고 자료 (엄수)
1) 동사 사전
{{REFERENCE.action_verbs}}
2) 문장 패턴
{{REFERENCE.sentence_patterns}}
3) 정량화 가이드
{{REFERENCE.metrics}}

입력 세션/텍스트
{{RAW_TEXT}}

지시사항
1) 이력서에 포함할 만한 "행동→결과" 성과만 식별하십시오.
2) 성과별로 루브릭(임팩트·복잡도·오너십·정량화·적합도)을 1–5점으로 평가하고 합계(total)를 계산하세요.
3) 약한 동사(예: 했다/만들었다/참여했다)는 금지. reference의 강한 동사로 교체 제안하세요.
4) 수치가 없으면 합리적 범위를 추정하여 "~" 표기와 assumptions를 함께 제시하세요.
5) sentence-patterns의 패턴 1/2를 기본으로 적용 가능한지 여부를 flag로 표기하세요.

출력(JSON Lines)
{
  "id": "string",
  "title": "강한 동사로 시작하는 성과 제목",
  "type": "feature|bugfix|optimization|refactoring|documentation",
  "summary": "2-3문장 요약 (행동→결과, 수치 포함)",
  "tech_stack": ["Java", "AWS"],
  "metrics": ["응답 시간 60% 단축"],
  "score": {"impact":1-5, "complexity":1-5, "ownership":1-5, "quant":1-5, "relevance":1-5, "total":0-25},
  "pattern_fit": {"p1": true|false, "p2": true|false},
  "assumptions": ["추정 근거 (있는 경우)"],
  "recommended": true|false   // total ≥ 16 권장
}

포함 기준
- total ≥ 16 이고, 약한 동사 미사용, summary 내 정량 지표 ≥ 1개.
```

---

### Prompt B: 성과 목록 → 그룹핑/연결성 분석

```
## 작업: 성과 그룹핑 및 대표 제목 생성

입력 성과(JSON Array)
{{ACHIEVEMENTS_JSON}}

연관 판단 기준 및 가중치
- 파일/모듈 공유 0.4, 키워드 유사 0.3, 시간 근접 0.2, 연속 작업 언급 0.1
- threshold ≥ 0.5 를 하나의 그룹으로 묶고 Union-Find로 연결하십시오.

출력(JSON)
{
  "groups": [
    {
      "title": "강한 동사 기반 대표 제목",
      "achievementIds": ["id1", "id2"],
      "period": {"start": "YYYY-MM-DD", "end": "YYYY-MM-DD"},
      "techStack": ["공통 기술"],
      "summary": "2-3문장 그룹 요약",
      "bullets": [
        "문장 패턴 1/2를 따른 1-3개 불릿 (수치 포함)"
      ]
    }
  ],
  "ungrouped": ["idN"]
}

제약
- 제목/불릿은 action-verbs의 강한 동사 사용.
- bullets는 sentence-patterns 패턴 1 또는 2를 따르고, metrics의 지표를 1개 이상 포함.
```

---

### Prompt C: 성과/그룹 → 최종 불릿 생성

```
## 작업: 이력서 불릿 생성 (액션→결과, 정량화)

컨텍스트
- Role: {{CONTEXT.role}} / Seniority: {{CONTEXT.seniority}} / Target: {{CONTEXT.target_role}}
- 언어: {{CONTEXT.lang}}

참고 자료
1) 동사 사전: {{REFERENCE.action_verbs}}
2) 문장 패턴: {{REFERENCE.sentence_patterns}}
3) 정량화 가이드: {{REFERENCE.metrics}}

입력
{{ACHIEVEMENT_OR_GROUP_JSON}}

지시사항
1) 각 성과에 대해 1–3개의 불릿을 생성하세요. 각 불릿은 2줄 이내.
2) 불릿은 강한 동사로 시작하고, sentence-patterns의 패턴 1 또는 2를 적용하십시오.
3) 결과(지표)를 명시하고, 지표가 모호한 경우 합리적 추정치를 "~"와 함께 제시하며 assumptions에 근거를 남기세요.
4) 기술 스택은 문장 내 자연스럽게 녹여 쓰되, 나열식은 피하십시오.
5) 동일 의미의 중복 표현을 제거하고, 능동태를 사용하십시오.

출력(JSON)
{
  "title": "강한 동사 제목",
  "bullets": ["불릿1", "불릿2"],
  "assumptions": ["추정 근거 (있는 경우)"]
}

셀프 체크리스트 (통과하지 못하면 수정보완 후 출력)
- [ ] 강한 동사 사용 (action-verbs Level 3+)
- [ ] 정량 지표 포함 (metrics)
- [ ] 패턴 1/2 적용
- [ ] 중복/군더더기 없음, 능동태
- [ ] 불릿당 2줄 이내
```

---

### Prompt D: 기존 이력서 → 개선 제안/재작성

```
## 작업: 기존 이력서 개선 및 재작성 불릿 제안

참고 자료
- 동사 사전: {{REFERENCE.action_verbs}}
- 문장 패턴: {{REFERENCE.sentence_patterns}}
- 정량화 가이드: {{REFERENCE.metrics}}
- 섹션 가이드: {{REFERENCE.section_guidelines}}

입력 이력서 (Markdown 또는 텍스트)
{{RESUME_TEXT}}

지시사항
1) 섹션별(자기소개/경력/프로젝트/기술/교육)로 약한 동사, 정량 미포함, 패턴 미적용 항목을 식별하십시오.
2) 표 형태로 현재 표현 → 개선안 → 이유를 제시하고, 이어서 재작성 불릿(1–3개)과 정량 지표를 포함한 최종 제안을 제공합니다.
3) 개선안은 action-verbs의 강한 동사와 sentence-patterns의 패턴 1/2를 기본으로 적용하십시오.
4) 치환/삭제/추가 제안을 diff 스타일(+/-)로도 함께 제시하십시오.

출력(Markdown)
## 이력서 개선 제안

### [섹션명]
| 현재 | 개선안 | 이유 |
|------|--------|------|
| "..." | "..." | "약동사 → 강동사, 정량화" |

#### 재작성 불릿
- 불릿1
- 불릿2

> 가정/추정: ...
```

---

### Case 7: `generate --from-sessions` - 세션 기반 생성

최근 Claude Code 세션을 분석하여 이력서에 작성할 성과를 자동 식별하고 초안을 생성합니다.

**인자 파싱**:

```
OPTIONS:
  --source=SOURCE       데이터 소스 (local|claude-mem, 기본: local)
  --since=YYYY-MM-DD    시작 날짜 지정
  --days=N              최근 N일 (기본: 30)
  --project=NAME        프로젝트 필터
  --dry-run             미리보기 (파일 생성 안 함)
```

**Step 1**: 분석 기간 계산

- `--since` 지정 시: 해당 날짜부터 오늘까지
- `--days` 지정 시: 오늘 기준 N일 전부터
- 기본: 최근 30일

**Step 2**: 데이터 소스에 따른 세션 수집

**2-A. --source=local (기본)**:

1. 세션 파일 검색:

```bash
# 분석 기간 내 수정된 세션 파일 (최대 20개)
find ~/.claude/projects -maxdepth 2 -name "*.jsonl" -type f -mtime -${DAYS} 2>/dev/null | \
  grep -v "agent-" | \
  xargs ls -lt 2>/dev/null | head -20 | awk '{print $NF}'
```

2. 프로젝트 필터 적용:
   - `--project` 옵션 지정 시: 경로에 프로젝트명이 포함된 파일만 선택

3. 세션 프롬프트 추출:

```bash
# 사용자 메시지만 추출 (스킬 명령 제외)
grep '"type":"user"' "${SESSION_FILE}" | \
  jq -r 'select(.message.content | type == "string") |
         select(.message.content | test("^<(local-command|command-name)") | not) |
         .message.content' 2>/dev/null | head -50
```

4. 서브에이전트 병렬 분석:
   - `subagent_type: general-purpose`, `model: haiku` 사용
   - `run_in_background: true` 설정
   - 각 세션에 대해 이력서 적합성 분석

**서브에이전트 프롬프트**:

```markdown
## 이력서 성과 분석 작업

다음 세션 대화에서 이력서에 작성할만한 성과를 추출하세요.

### 분석 기준

RESUME_KEYWORDS: 구현, 개발, 설계, 구축, 최적화, 개선, 리팩토링, 마이그레이션, 자동화

### 세션 내용

{SESSION_PROMPTS}

### 출력 (JSON)

{
"score": 1-10,
"type": "feature|bugfix|optimization|refactoring|documentation",
"title": "성과 제목 (강한 동사 사용)",
"summary": "2-3문장 요약",
"tech_stack": ["기술1", "기술2"],
"recommended": true/false
}

성과가 없으면 {"recommended": false}만 반환
```

5. 결과 취합 및 Step 3으로 진행

**2-B. --source=claude-mem**:

```
타입별 병렬 검색:
- search(type="🟣", dateStart, dateEnd)  → feature 관찰
- search(type="🔴", dateStart, dateEnd)  → bugfix 관찰
- search(type="⚖️", dateStart, dateEnd)  → decision 관찰
- search(type="✅", dateStart, dateEnd)  → change 관찰

프로젝트 필터 적용 시:
- --project 인자로 project 파라미터 추가
```

**Step 3**: 이력서 적합성 필터링

```
RESUME_KEYWORDS (포함 시 적합):
  '구현', '개발', '설계', '구축', '도입', '적용',
  '최적화', '개선', '리팩토링', '고도화',
  '마이그레이션', '전환', '이관',
  '자동화', 'CI/CD', '파이프라인', '배포',
  '문서화', '가이드', '스킬'

EXCLUDE_KEYWORDS (포함 시 제외):
  '조회', '확인', '검색', '질문', '리서치', '탐색'

필터링 로직:
1. 타입 체크: 🟣, 🔴, ⚖️, ✅ 중 하나
2. 키워드 체크: RESUME_KEYWORDS 중 하나 포함
3. 제외 체크: EXCLUDE_KEYWORDS 포함 시 제외
```

**Step 3.5**: 연관성 분석 & 그룹핑

서브에이전트 분석 결과에서 여러 세션을 하나의 성과로 그룹핑합니다.

**연관 점수 계산** (0.0 ~ 1.0):

| 요소           | 가중치 | 계산 방법                     |
| -------------- | ------ | ----------------------------- |
| 파일 경로 공유 | 0.4    | 같은 폴더/파일 다룸           |
| 키워드 유사도  | 0.3    | 제목/설명 키워드 일치율       |
| 시간 근접성    | 0.2    | 1일: 0.2, 3일: 0.15, 7일: 0.1 |
| 명시적 연결    | 0.1    | 이전 작업 언급                |

**그룹핑 규칙**:

- threshold >= 0.5인 성과 쌍을 연결
- Union-Find 알고리즘으로 그룹 형성
- 각 그룹에 대표 제목 생성 (가장 높은 score 항목 기반)

**그룹 대표 제목 생성 규칙**:

1. 가장 높은 score를 가진 성과의 title 사용
2. 그룹 내 공통 기술 스택 추출
3. 세션 수 표시: "{title} ({N}개 세션)"
4. 기간 표시: 그룹 내 최초~최종 날짜

**서브에이전트 프롬프트 (그룹핑용)**:

```markdown
## 이력서 성과 그룹핑 작업

다음 성과 목록에서 연관된 항목을 그룹핑하세요.

### 연관 판단 기준

- 같은 기능/모듈 작업 (파일 경로 공유)
- 유사한 작업 내용 (키워드 일치)
- 연속된 작업 (시간 근접)
- 작업 연속성 언급 ("이전 작업 이어서", "후속 작업")

### 성과 목록 (JSON)

{ACHIEVEMENTS_JSON}

### 출력 (JSON)

{
"groups": [
{
"title": "그룹 대표 제목 (강한 동사 사용)",
"achievementIds": ["id1", "id2"],
"techStack": ["기술1", "기술2"],
"summary": "그룹 전체 요약 (2-3문장)"
}
],
"ungrouped": ["독립 성과 id"]
}
```

**Step 4**: Reference 패턴 적용

- `reference/sentence-patterns.md` 패턴 로드
- `reference/action-verbs.md` 약한 동사 → 강한 동사 변환
- 각 성과에 적절한 패턴 적용하여 문장 생성

```
변환 예시:
입력: "resume-ref 스킬 구현 완료"
출력: "Claude Code 기반 이력서 자동 생성 스킬을 설계/구현하여 개발자 이력서 작성 효율화"

약한 → 강한 동사 변환:
"만들었다" → "설계", "구축"
"고쳤다" → "개선", "최적화"
"바꿨다" → "전환", "마이그레이션"
```

**Step 5**: 초안 파일 생성

출력 경로: `./resume-drafts/session-analysis-YYYY-MM-DD.md`

`--dry-run` 지정 시: 터미널에 미리보기만 출력 (파일 생성 안 함)

**출력 파일 템플릿**:

```markdown
# 이력서 초안 (세션 분석 기반)

> 생성일: {{generatedAt}}
> 분석 기간: {{period.start}} ~ {{period.end}}
> 분석 세션: {{sessionsAnalyzed}}개
> 식별된 성과: {{achievementsFound}}개 ({{groupsCreated}}개 그룹)

---

## 주요 성과

{{#each groups}}

### {{index}}. {{title}}

**기간**: {{period.start}} ~ {{period.end}}
**관련 세션**: {{totalSessions}}개

{{#each bullets}}

- {{this}}
  {{/each}}

**기술 스택**: {{techStack}}

{{#if needsReview}}

> 💡 **보완 권장**: {{#each reviewHints}}- {{this}} {{/each}}
> {{/if}}

---

{{/each}}

{{#if ungroupedAchievements}}

## 개별 성과

{{#each ungroupedAchievements}}

### {{index}}. {{title}}

**날짜**: {{date}}
**타입**: {{type}}

{{#each bullets}}

- {{this}}
  {{/each}}

**기술 스택**: {{techStack}}

---

{{/each}}
{{/if}}

## 기술 스택 요약

{{techStackSummary}}

---

## 다음 단계

1. [ ] 각 성과의 정량적 수치 보완
2. [ ] 약한 표현 → 강한 표현 재검토
3. [ ] 실제 이력서에 반영

---

> 이 초안은 Claude Code 세션 분석을 기반으로 자동 생성되었습니다.
> Reference 패턴이 적용되었으나, 최종 검토 후 사용하세요.
```

---

## 에러 처리

| 상황              | 메시지                                                                    |
| ----------------- | ------------------------------------------------------------------------- |
| Reference 없음    | ⚠️ Reference 문서가 없습니다. 먼저 분석을 실행해주세요.                   |
| Git 없음          | ⚠️ Git 저장소가 아닙니다. --from-git 옵션을 사용할 수 없습니다.           |
| 파일 없음         | ❌ 파일을 찾을 수 없습니다: {path}                                        |
| claude-mem 미연결 | ⚠️ claude-mem MCP가 연결되어 있지 않습니다. (--source=claude-mem 사용 시) |
| 세션 파일 없음    | ⚠️ ~/.claude/projects에서 세션 파일을 찾을 수 없습니다.                   |
| jq 미설치         | ⚠️ jq가 설치되어 있지 않습니다. brew install jq                           |
| 세션 데이터 없음  | ⚠️ 분석 기간 내 세션 데이터가 없습니다.                                   |
| 적합한 성과 없음  | ⚠️ 이력서에 작성할 만한 성과를 찾지 못했습니다.                           |

## 예시

```bash
# 문장 패턴 참고
/resume-ref patterns

# 대화형으로 이력서 생성
/resume-ref generate

# Git 커밋 기반 이력서 생성
/resume-ref generate --from-git

# 세션 기반 이력서 생성 (로컬 세션 파일, 최근 30일)
/resume-ref generate --from-sessions

# 세션 기반 - 로컬 세션 파일 분석 (기본값, 명시적 지정)
/resume-ref generate --from-sessions --source=local

# 세션 기반 - claude-mem MCP 사용
/resume-ref generate --from-sessions --source=claude-mem

# 세션 기반 - 최근 60일
/resume-ref generate --from-sessions --days=60

# 세션 기반 - 특정 날짜 이후
/resume-ref generate --from-sessions --since="2026-01-01"

# 세션 기반 - 특정 프로젝트만
/resume-ref generate --from-sessions --project="dev-assistant"

# 세션 기반 - 미리보기 (파일 생성 안 함)
/resume-ref generate --from-sessions --dry-run

# 기존 이력서 개선
/resume-ref improve ~/resume.md
```
